# Writerdeck: keep Alt+Left/Right as arrow keys for word navigation.
# Default linux-keys-bare.inc maps them to Decr/Incr_Console, which Qt evdev
# turns into Key_Escape and toggles edit/preview on key release.
alt keycode 105 = Left
alt keycode 106 = Right
